###> Eclipse project files
/.settings/
/.buildpath
/.project
###< Eclipse project files

##############################
# Ansible meta data
##############################
/ansible.d/playbook/roles/*/
!/ansible.d/playbook/roles/index
/ansible.d/log/
/ansible.d/playbook.retry
/ansible.d/inventory
/ansible.d/playbook/vars/jenkins-slave.yaml

##############################
# Vagrant meta data
##############################
/.vagrant/
/vault.d/secrets.json
/vault.d/certs/*
/vagrant.d/vagrantConfig.yaml
/vagrant.d/puppet/Puppetfile.lock
/vagrant.d/puppet/.librarian
/vagrant.d/puppet/.tmp
/ssh.EXE.stackdump
/.env
/var/
###< Vagrant files

### Here has confidential credentials
### The folder is stored in DropBox /Projects/VS_DevOpsServer/
/jenkins.d/casc/vs_devops.yaml


# +----------------------------+
# |    Keep .gitkeep files     |
# +----------------------------+-------------------------------+
# | To keep an empty directory, add a .gitkeep file to it      |
# | and ignore the directory with a wildcard:                  |
# |                                                            |
# |     /path/to/dir/*                                         |
# |                                                            |
# | this will ignore the content but not the director itself.  |
# +------------------------------------------------------------+
!/**/.gitkeep


##############################
# Terraform meta data
##############################
.terraform/
terraform.tfstate
terraform.tfstate
terraform.tfstate.backup
.terraform.lock.hcl

#######################################
# Monitoring Hosts Configurations
#######################################
icingaConfig.yml
nagiosConfig.yml


##############################
# Angular Dashboard App
##############################
/vagrant/gui/vs-devops-dashboard/node_modules/

